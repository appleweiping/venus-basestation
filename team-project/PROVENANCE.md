# Team 28 GitLab Snapshot Provenance

This directory is a public-safe snapshot of the local Team 28 GitLab checkout copied from:

- Local source: `D:/Undergraduate_project_netherlands/venus-team-28-gitlab`
- GitLab remote: `git@gitlab.tue.nl:d.gyftakis/venus-team-28.git`
- Source branch at copy time: `main`
- Source HEAD at copy time: `44b08fde294700048a4f286c706800f2b92e306a`

Copy policy:

- `.git/` metadata was intentionally not copied.
- Personal `sftp.json` files are excluded; `sftp.json.example` is retained as course setup documentation.
- This repository is public on GitHub, so do not add real credentials, private keys, or local board-specific secrets here.
- Generated Doxygen HTML under `libpynq-5EID0-2023-v0.3.0/doc/html/` is excluded to keep the archive source-focused.
- Module branch work from GitLab is represented under `module-branches/`.
